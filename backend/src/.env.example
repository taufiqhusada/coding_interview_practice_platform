# MongoDB Configuration (Optional - leave empty to use in-memory storage)
# If not provided, the app will use in-memory storage (data lost on restart)
MONGO_URI=

# Required: OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_GPT_MODEL=gpt-4o-mini

# Required: JWT Secret Key (can be any random string - just type random words!)
JWT_SECRET_KEY=your_jwt_secret_key_here
